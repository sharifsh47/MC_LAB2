Write a C function configTimer1A() that configures Timer1A for 16-bit compare mode, upwards
counting, with optimum prescaler. Set the interval load value to periodically to give a timeout
every second.
